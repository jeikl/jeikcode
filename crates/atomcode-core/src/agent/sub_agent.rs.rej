--- crates/atomcode-core/src/agent/sub_agent.rs
+++ crates/atomcode-core/src/agent/sub_agent.rs
@@ -12,6 +12,7 @@ use tokio_util::sync::CancellationToken;
 
 use crate::config::Config;
 use crate::conversation::Conversation;
+use crate::hook::HookRegistry;
 use crate::provider::LlmProvider;
 use crate::tool::{ToolContext, ToolRegistry};
 use crate::turn::event::{TurnEvent, TurnResult};
@@ -87,6 +88,7 @@ impl SubAgentTask {
             context: ctx,
             config: config.clone(),
             permission,
+            hook_registry: HookRegistry::new(), // Sub-agents don't use hooks for now
             recently_edited_files: Vec::new(),
             recent_calls: Vec::new(),
             file_read_counts: std::collections::HashMap::new(),
