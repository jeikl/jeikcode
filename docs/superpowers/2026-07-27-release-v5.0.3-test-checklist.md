# release/v5.0.3 整体测试清单

> 基于最新分支代码（`atomcode-core` 已整个退役；含并行落地的 round-cap / cost / webui / session / skills / TLS 等）。
> 相对 main：**105 commits，291 文件，+16k/-68k**。全程未真机 → 上线前请逐条过。
> 环境：webui + TUI（cli）都要覆盖；真 provider（GLM/deepseek/openrouter…）。
> 图标：🔴=退役 core 高风险改动（重点）｜🟡=并行新特性｜⚪=冒烟

---

## 0. 前置
- [ ] `cargo build --workspace` 干净（release 也构建一次）
- [ ] `cargo test --workspace` 通过（webui embedded-asset 2 个失败是缺 `webui/dist` 构建产物，属环境性，可忽略）
- [ ] 准备一个**文字模型**当主模型 + 一个**视觉模型**配 `vision_preprocessor_provider`（验图片剥离必需）

## 1. ⚪ P0 冒烟
- [ ] webui 起、登录、选模型、发消息、工具执行全链路
- [ ] TUI 起、发消息、工具执行、审批
- [ ] 切换 provider / model 正常

## 2. 🔴 会话传输（C2：daemon 传输已改 kernel Vec\<Message\>，最大风险面）
- [ ] webui 新建会话发多轮 → 正常
- [ ] **续聊历史会话**（/resume 选旧）→ 工具调用/结果/图片缩略图/thinking 全部正确渲染
- [ ] 会话中 **Esc / Ctrl+C / Cancel 中止** turn → 干净中止不卡死，历史不丢，未完成工具调用被补 `(cancelled)`
- [ ] **压缩后续聊**（跑到高上下文自动压缩，或手动 /compact）→ 继续对话正常，旧摘要（cold summary）保留、不重复、不错序
- [ ] **页面刷新重连** → 会话恢复，轮次/时间戳不跳
- [ ] 两个 tab / 多窗口同一会话 → 不串台
- [ ] 🟡 **老会话恢复**（session fixes）：打开一个 legacy 格式旧会话（缺 turn counts / 空 native stub）→ 能正常恢复不报错
- [ ] 🟡 busy 时连续点"继续" → fork 有上限，不失控

## 3. 🔴 Vision 图片预处理（B + 修复 + webui sidecar）
主模型=文字模型，`vision_preprocessor_provider`=视觉模型：
- [ ] webui `/chat` 贴图 → 生成 `[图片内容（由 <vl_model> 识别）]\n<描述>`，原图**不进文字模型**（不报 400）
- [ ] webui `/live` 贴图 → 同上
- [ ] 🔴 **错配 case**：`vision_preprocessor_provider` 填一个 **config 里不存在的名字** → 显示 `[图片识别失败]` 且**不报 400**（这是修复点 `70c4c338`，webui + TUI 都要验）
- [ ] `vision_preprocessor_provider` 留空/不配 → 贴图原样透传（不加 marker）
- [ ] vl_model 带 `vendor/` 前缀（`Qwen/Qwen3-VL`）→ toast/marker 显示去前缀的 `Qwen3-VL`
- [ ] TUI 贴图 → 同样验（cli 走独立 `VlImagePreprocessor`）
- [ ] 🟡 **webui 刷新后图片还在**（sidecar）：贴图→VL 识别→刷新页面 → 缩略图仍在（display-only sidecar，零 context 成本）
- [ ] 🟡 webui 图片上传后**显示原始消息+图**，不是 VL caption（`fix(webui): show original message+image`）
- [ ] 🟡 sync 路径上传图 → 刷新后图不丢

## 4. 🔴 Permission 审批（D：PermissionDecision 迁 capabilities）
webui **Build 模式**：
- [ ] 触发需审批工具（工作区外 bash / 写文件）→ 弹审批
- [ ] **Allow**（一次）→ 放行，同类下次再弹
- [ ] **Always allow** → 放行，同类下次不再弹（本会话记住）
- [ ] **Deny** → 拒绝，工具不执行
- [ ] Plan 模式：只读放行、写被挡
- [ ] TUI 审批流同样验一遍

## 5. 🔴 Skills（E1：core::skill 迁 capabilities::skills + 并行 skills 特性）
- [ ] webui `/skills` 列出用户可调用技能
- [ ] 装一个含 skill 的插件后 `/skills` → 插件技能出现（`<plugin>:<skill>` 命名空间）
- [ ] 🟡 `/skills a b c` **贪婪匹配多个** skill 名 → 都加载 + 回显已加载（`feat(skills)`）
- [ ] 🟡 同一 skill 不同写法 → **去重**不双注入（`fix(skills)`）
- [ ] ⚠️ 已知 LOW 差异：`ATOMCODE_HOME` 重定位或 sudo 环境下，webui `/skills` 可能漏 `.atomcode/*` 技能（capabilities reload 用 `dirs::home_dir()` 单一解析）。普通安装不受影响

## 6. 🔴 Plugin（E2：core::plugin 已删，全走 capabilities::plugin）
- [ ] marketplace 添加 / 列出 / 更新 / 移除
- [ ] 插件安装 / 卸载 / 列出
- [ ] 插件贡献的 hooks 生效（信任门：装后需 trust 才跑）

## 7. 🔴 /compact（A：provider 迁原生 factory）
- [ ] webui / daemon 触发 `/compact` → 返回 applied + before/after tokens 合理，无 panic
- [ ] 🔴 用**不存在的 provider 名** /compact → 干净报错 `Provider 'x' not found`（修复点 `ec1ff49e`）

## 8. 🟡 round-cap 检查点（新特性：max_rounds 到达时交互式续/停）
- [ ] 配 `[coding] max_rounds` 较小值，跑一个多轮任务撞上限 → 弹**检查点**（继续/停止）
- [ ] 选**继续** → 接着跑
- [ ] 选**停止** → 干净停
- [ ] 检查点弹出时 **Ctrl+C** → 取消语义正确（不是误继续）
- [ ] 初始 TUI agent 首轮也能触发检查点

## 9. 🟡 Cost / 定价（新特性：models.dev 定价 + 归因）
- [ ] `/cost` 或用量显示 → token 数正确
- [ ] 模型定价从 models.dev 解析 → 费用估算合理
- [ ] 多 provider/model 混用一轮 → 用量**按 provider+model 归因**正确（`fix(cost)`）
- [ ] 模型重组后用量归因不错乱

## 10. 🟡 webui 专项（大量并行修复）
- [ ] 发送消息**乐观显示**（sync 路径立即离开落地页，不等服务端）
- [ ] 多窗口 SSE 广播时**用户选的模型不被覆盖**（#1201）
- [ ] webui footer / 会话列表显示**共享 TUI 的工作目录**（不覆盖读路径）
- [ ] 内容列响应式加宽 + 暗色表格边框对比正常
- [ ] webui 与 TUI 同目录会话联动正常

## 11. 🟡 TUI 专项
- [ ] **子任务进度**在 footer 常驻显示（subagent 委派时）
- [ ] pending message 交互改进正常
- [ ] 提交时**换行的 user echo 保留**（`fix(tuix)`）

## 12. 🟡 平台 / TLS
- [ ] **Windows**：起 webui/TUI + 登录 + 发消息（TLS 走 SChannel/native-tls，绕 rustls 指纹拦截 `fix(tls)`）
- [ ] Windows：插件安装、审批、贴图各验一遍（core 退役后 Windows 依赖已清，重点回归）
- [ ] macOS / Linux：冒烟

## 13. 🔴 诊断 / Trace（trace 已从 core 搬到 daemon）
- [ ] `ATOMCODE_TUIX_LOG=/tmp/atomcode.log` 起服务跑一轮 → 日志正常写（daemon `ctrace!` + tuix 各自 append 同文件不冲突）

---

## 回归重点（退役 core 影响面，出问题优先查这里）
1. **贴图**（vision 迁移 + 错配修复 + sidecar）—— 最容易出问题
2. **续聊 / 压缩 / 取消**（传输层改 kernel buffer）
3. **审批**（PermissionDecision 换 crate）
4. **/skills 列表含插件技能**（reload 的 plugin 层是手动补的）
5. **Windows**（依赖清理 + TLS 后端）

## 已知非阻塞项（review 发现，不 block 上线）
- daemon 少声明 capabilities 的 `skills`/`session` feature（靠 coding 传递，脆不崩）
- E1 loose-skill 在 ATOMCODE_HOME/sudo 下解析差异（见 §5）
- 若干过期 doc 注释提到已删的 `core::*`（仅注释）
