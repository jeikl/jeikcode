{
  "name": "site",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "build:css": "tailwindcss -c tailwind.config.js -i input.css -o dist.css --minify",
    "watch:css": "tailwindcss -c tailwind.config.js -i input.css -o dist.css --watch",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "puppeteer": "^24.40.0",
    "tailwindcss": "^3.4.19"
  }
}
