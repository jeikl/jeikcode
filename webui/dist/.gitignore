# Built frontend assets are NOT committed (generated by `npm run build`).
# Only the placeholder index.html is tracked so rust-embed compiles without
# requiring a prior frontend build.
*
!.gitignore
!index.html
